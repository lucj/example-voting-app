apiVersion: v1
kind: Service
metadata:
  labels:
    app: vote
  name: vote
spec:
  type: NodePort
  ports:
  - name: "vote-service"
    nodePort: 31001
    port: 5000
    targetPort: 80
  selector:
    app: vote
